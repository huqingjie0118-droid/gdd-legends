# 复制为 .env 并按需修改（不配置也能量运行，邮件走本地 dev 回退）
PORT=3000
BASE_URL=http://localhost:3000

# 真实邮件（可选）：配置后自动走 SMTP，否则写入 server/.mailbox/ 并打印链接
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587
# SMTP_SECURE=0
# SMTP_USER=your@qq.com
# SMTP_PASS=授权码
# MAIL_FROM=noreply@legends.local
